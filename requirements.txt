# Pinned to the versions used to produce the published results.
# Newer versions will very likely work; these are exact for reproducibility.
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
matplotlib==3.10.8
SALib>=1.5.0
openpyxl>=3.1.0
pytest>=7.0
